# M3 independent repetition — minimal public evidence

This directory contains the deterministic output of the existing sanitized live-evidence gate for a second independent NOT FOR MANUFACTURING fixture.

Public result:

- BEFORE: 5 components, 3 networks, one blocking `DECOUPLING_DISTANCE:J2:+5V` finding;
- repair: the sole allow-listed `ADD_LOCAL_BYPASS_CAP` plan;
- AFTER: 6 components, 3 networks, target finding resolved;
- save/close/reload and independent readback verified;
- ERC/connectivity/containment/strict DRC passed;
- fresh rating: `suitable_for_low_risk_prototype`;
- unrelated risk severity did not worsen.

The output filename and schema retain the v0.1.0 M2 gate vocabulary for compatibility. The `case` field identifies this as the M3 independent sensor-adapter repetition. Raw receipts, project identity, object IDs, workstation paths, screenshots and private adapter data are excluded. This is not Manufacturing Release or physical functional proof.
